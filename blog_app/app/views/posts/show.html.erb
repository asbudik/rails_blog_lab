<h2><%= @post.title %></h2>
<% @post.tags.each do |tag| %>
  <%= tag.name %>
<% end %>

<p><%= @post.author %></p>
<p><%= @post.description %></p>

<a href="<%= @post.id %>/edit"><button>Edit</button></a>